Add complete authentication system and property management features

Major Features Added:

1. Authentication System
   - Auth context with JWT token management
   - Login page with email/password
   - Registration page with role selection (USER/AGENT)
   - Protected routes middleware
   - Token refresh mechanism
   - Logout functionality

2. Property Detail Page (/properties/[id])
   - Full property information display
   - Image gallery with thumbnails
   - Agent contact information
   - Property amenities list
   - Save/favorite functionality
   - Share property feature
   - Book inspection modal

3. Create Property Form (/properties/new)
   - Multi-step form wizard (4 steps)
   - Property details (title, type, description)
   - Pricing and room details
   - Image upload with preview
   - Amenity selection
   - Review before submit
   - Protected route for agents only

4. Search & Filters (/search)
   - Full-text search functionality
   - Price range filters
   - Bedroom/bathroom filters
   - Property type filter
   - Sort options (price, date, views)
   - Results grid with pagination
   - Active filters display

5. Saved Properties (/saved)
   - List all saved properties
   - Remove from saved
   - Property card display
   - Empty state UI
   - Protected route

6. Navigation Updates
   - Auth-aware navbar
   - User menu with dropdown
   - Login/register buttons
   - Logout functionality
   - Saved properties link
   - List property CTA for agents

Technical Changes:
- Added lib/auth.ts with complete auth context
- Added API_BASE export to lib/api.ts
- Updated layout.tsx with AuthProvider
- Added "use client" to page.tsx in doxxa-rentals
- Created multiple new page components
- Added protected route HOC
